import { defineMessages } from 'react-intl';

export default defineMessages({
  errorLoadingScope: {
    id: 'audience.dashboard.error.loadScope',
    defaultMessage: 'Error loading scope query',
  },
});
