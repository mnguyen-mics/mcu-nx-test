import { defineMessages } from 'react-intl';

const messages = defineMessages({
  users: {
    id: 'settings.tab.title.users',
    defaultMessage: 'Users',
  },
  labels: {
    id: 'settings.tab.title.labels',
    defaultMessage: 'Labels',
  },
});

export default messages;
