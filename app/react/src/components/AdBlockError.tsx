import * as React from 'react';

const AdBlockError: React.SFC = () => {
  return (
    <div className="mcs-centered-container">
      Please remove your AdBlock and refresh the page
    </div>
  );
};

export default AdBlockError;
